# Core
discord.py>=2.7.1,<3.0.0
aiosqlite>=0.21.0,<0.22.0
python-dotenv>=1.2.2,<2.0.0

# aiohttp NICHT pinnen – discord.py schränkt es bereits korrekt ein
# (>=3.9.0,<4). Ein eigener Pin blockiert nur Security-Updates.

# Nötig für RIDDLE_DISPLAY_TIMEZONE unter Windows / schlanken Containern:
# ohne tzdata wirft ZoneInfo("Europe/Berlin") ZoneInfoNotFoundError und
# _resolve_display_tz() fällt still auf UTC zurück.
tzdata>=2026.1; sys_platform == "win32" or platform_system == "Windows"

# Pillow: in riddle_*.py nirgends importiert – nur behalten, wenn ein
# anderer Cog es braucht.
Pillow>=11.0,<13.0